# 依赖
node_modules/

# 日志与临时产物
*.log
npm-debug.log*
.DS_Store
*.tmp
*.checkpoint.json

# 采集输出（NDJSON 结果落地默认写在这里，不入库）
out/
output/
*.ndjson

# 编辑器
.idea/
.vscode/
*.swp

# 指纹采集中间文件（WSL ↔ Windows 交换目录的本地副本）
/tmp-fingerprint/

# Realm 模块预打包缓存：键是绝对 file:// URL，与本机路径绑定。
# 提交它等于给别人一份命中率为 0 却仍要 readFileSync + JSON.parse 的负担
# （实测拖慢冷启动约 90ms）。用 npm run build:bundle 本地生成。
/src/realm/module-bundle.json
